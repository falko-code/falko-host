name: host
description: A starting point for Dart libraries or applications.
version: 1.0.0
resolution: workspace
environment:
  sdk: ">=3.13.0-0 <4.0.0"
dependencies:
  host_di:
  meta: ^1.18.3
dev_dependencies:
  host_lints:
  test: ^1.25.6
